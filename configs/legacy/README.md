# Legacy Configs

This directory contains pre-mainline WFCLLM experiment configs that no longer
belong to the default `configs/` surface.

Use the mainline configs under `configs/wfcllm/` for new runs.
