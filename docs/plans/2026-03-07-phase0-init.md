# 阶段零：工程初始化与规范确立 实现计划

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 搭建标准化项目框架，确立 CLAUDE.md 工程规范，创建包骨架，锁定依赖，整理 Git 分支。

**Architecture:** 先写 CLAUDE.md 规范文档，再按规范创建 `wfcllm/` 包骨架目录，然后导出 requirements.txt，最后整理 Git 分支（master → main → develop）。

**Tech Stack:** Python, pytest, tree-sitter, Conda, Git

---

### Task 1: 创建 CLAUDE.md 规范文档

**Files:**
- Create: `CLAUDE.md`

**Step 1: 创建 CLAUDE.md**

```markdown
# WFCLLM: 基于语句块语义特征的生成时代码水印

> 方案文档：`docs/基于语句块的语义特征的生成时代码水印方案.md`

## 项目架构

```
wfcllm/              # 主包
├── encoder/          # 阶段一：鲁棒语义编码器预训练
├── watermark/        # 阶段二：生成时水印嵌入
├── extract/          # 阶段三：提取与验证
└── common/           # 共享工具（AST 解析、配置等）

experiment/           # 前期实验代码（仅供算法逻辑参考）
tests/                # 测试代码
docs/                 # 文档与设计
data/                 # 数据
```

## Git 规范

**分支模型（Git Flow 简化版）：**
- `main`：稳定版本，里程碑合并
- `develop`：开发主线，日常合入
- `feature/*`：功能分支，从 develop 创建，PR 合入 develop
- `experiment/*`：实验分支，从 develop 创建，PR 合入 develop

**Commit 格式：** `<type>: <description>`

type 取值：feat / fix / refactor / test / docs / chore

## 测试规范

- 框架：pytest
- 目录：`tests/`，子包测试放 `tests/<subpackage>/`
- 命名：`test_<module>.py`，测试函数 `test_<行为描述>()`
- 要求：每个公共函数/类必须有对应测试
- 运行：`conda run -n WFCLLM pytest tests/ -v`

## 代码迁移规范

- `experiment/` 下的代码仅作为算法逻辑参考
- **严禁** `wfcllm/` 中 import `experiment` 下的任何模块
- 所有逻辑必须按 `wfcllm/` 的架构与编码规范重写

## 环境管理

- Conda 环境名：`WFCLLM`
- 依赖清单：`requirements.txt`
- 核心业务依赖（tree-sitter 等）精确锁定版本号
- CUDA / PyTorch 等硬件相关依赖不锁版本，视目标服务器适配
- **严禁** 在 `base` 环境中安装业务依赖或运行项目代码
```

**Step 2: 检查文件内容**

Run: `head -5 CLAUDE.md`
Expected: 看到标题行 `# WFCLLM`

**Step 3: Commit**

```bash
git add CLAUDE.md
git commit -m "docs: add CLAUDE.md with project conventions and standards"
```

---

### Task 2: 创建 wfcllm/ 包骨架

**Files:**
- Create: `wfcllm/__init__.py`
- Create: `wfcllm/encoder/__init__.py`
- Create: `wfcllm/watermark/__init__.py`
- Create: `wfcllm/extract/__init__.py`
- Create: `wfcllm/common/__init__.py`

**Step 1: 创建目录和文件**

```bash
mkdir -p wfcllm/encoder wfcllm/watermark wfcllm/extract wfcllm/common
```

`wfcllm/__init__.py`:
```python
__version__ = "0.1.0"
```

`wfcllm/encoder/__init__.py`: 空文件
`wfcllm/watermark/__init__.py`: 空文件
`wfcllm/extract/__init__.py`: 空文件
`wfcllm/common/__init__.py`: 空文件

**Step 2: 验证结构**

Run: `find wfcllm -type f | sort`
Expected:
```
wfcllm/__init__.py
wfcllm/common/__init__.py
wfcllm/encoder/__init__.py
wfcllm/extract/__init__.py
wfcllm/watermark/__init__.py
```

**Step 3: 验证包可导入**

Run: `conda run -n WFCLLM python -c "import wfcllm; print(wfcllm.__version__)"`
Expected: `0.1.0`

**Step 4: Commit**

```bash
git add wfcllm/
git commit -m "feat: scaffold wfcllm package with phase-based subpackages"
```

---

### Task 3: 创建 requirements.txt

**Files:**
- Create: `requirements.txt`

**Step 1: 创建 requirements.txt**

从 Conda 环境 `WFCLLM` 中获取实际安装版本，按以下分类组织：

```
# AST 解析
tree-sitter==0.25.2
tree-sitter-python==0.25.0

# 数据处理
datasets==4.6.1
pandas==3.0.1
numpy==2.4.2
pyarrow==23.0.1

# 网络与 Hub
huggingface_hub==1.5.0
requests==2.32.5

# 工具
tqdm==4.67.3
PyYAML==6.0.3
rich==14.3.3
typer==0.24.1

# 深度学习（不锁版本，视目标服务器适配）
# torch
# transformers

# 测试
pytest==9.0.2
```

**Step 2: 验证格式**

Run: `conda run -n WFCLLM pip install --dry-run -r requirements.txt 2>&1 | tail -3`
Expected: 显示 "would install" 或 "already satisfied"，无格式错误

**Step 3: Commit**

```bash
git add requirements.txt
git commit -m "chore: add requirements.txt with pinned dependency versions"
```

---

### Task 4: 整理 Git 分支

**背景：** 当前只有 `master` 分支，无远程仓库。需要创建 `main` 和 `develop`。

**Step 1: 将 master 重命名为 main**

```bash
git branch -m master main
```

**Step 2: 从 main 创建 develop 分支**

```bash
git branch develop main
```

**Step 3: 切换到 develop 分支**

```bash
git checkout develop
```

**Step 4: 验证分支状态**

Run: `git branch`
Expected:
```
  main
* develop
```

---

### Task 5: 最终验证

**Step 1: 验证项目结构**

Run: `ls -la` 在项目根目录
Expected: 看到 `CLAUDE.md`, `requirements.txt`, `wfcllm/`, `experiment/`, `tests/`, `docs/`, `data/`

**Step 2: 验证包导入**

Run: `conda run -n WFCLLM python -c "from wfcllm import encoder, watermark, extract, common; print('OK')"`
Expected: `OK`

**Step 3: 验证现有测试不受影响**

Run: `conda run -n WFCLLM pytest tests/ -v --tb=short 2>&1 | tail -10`
Expected: 所有现有测试仍然通过

**Step 4: 验证 Git 日志**

Run: `git log --oneline -5`
Expected: 看到 3 个新 commit（CLAUDE.md、wfcllm 骨架、requirements.txt）
