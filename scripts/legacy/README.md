# Legacy Scripts

These scripts are retained as archive guidance for the pre-mainline WFCLLM pipeline.
The implementation they used to call has been moved to
`archive/legacy_wfcllm_2026_07/code/`.

New default workflows should use `run.py` with the mainline WFCLLM phases and
the scripts under `scripts/` outside this directory.
