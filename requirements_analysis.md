# 《程序设计实验》需求分析文档

**题目：** 基于语句块语义特征的生成时代码水印系统  
**班级：** [填写]  
**学号：** [填写]  
**姓名：** [填写]  
**指导教师：** [填写]  
**2023 年 9 月**

## 1、任务概述

### 1.1、目标

随着代码大语言模型在程序生成任务中的广泛应用，如何对模型生成代码进行来源标识与后续验证，已成为值得研究的问题。本实验拟设计并实现一个基于语句块语义特征的生成时代码水印系统，在代码生成过程中嵌入隐式水印，并在后续阶段完成检测与验证。其主要目标如下：

1. 设计并实现代码语义编码器，对代码语句块进行语义特征表示，为后续水印嵌入和提取提供基础。
2. 实现生成时代码水印嵌入功能，在代码生成模型输出代码时，将特定水印信息融入生成结果中。
3. 实现水印提取与验证功能，对待测代码进行分析，判断其是否包含预设水印。
4. 建立较完整的实验流程，覆盖编码器训练、水印嵌入、水印提取三个阶段。
5. 为代码版权保护、生成内容溯源以及代码生成安全研究提供实验支撑。

本软件属于一个相对独立的实验系统，主要由语义编码器训练模块、水印嵌入模块和水印提取模块组成。系统整体流程可概括为：

代码数据集 → 语义编码器训练 → 生成代码并嵌入水印 → 输出含水印代码 → 水印提取与验证

本系统主要面向课程实验与算法验证场景，可在本地离线环境中运行，不依赖在线服务完成核心功能。

### 1.2、系统（或用户）的特点

本系统具有以下特点：

1. 面向代码生成场景，具有较强的实验性和一定的研究意义。
2. 系统按训练、嵌入、提取三个阶段组织，模块划分清晰，便于实现、测试和维护。
3. 相较于仅依赖文本表面特征的方法，本系统更关注代码语义特征，对变量重命名、格式变化等操作具有一定鲁棒性。
4. 支持离线运行，适合实验室环境及课程实验场景。
5. 以命令行方式作为主要交互手段，便于配置参数和重复执行实验。

本系统的主要用户包括：

1. 程序设计实验课程中的学生；
2. 指导教师；
3. 对代码生成与代码水印技术感兴趣的研究人员。

系统在开发和调试阶段使用频率较高，在实验验证和结果分析阶段根据需要运行。

### 1.3、假定和约束

本实验在需求分析和系统开发过程中，作如下假定与约束：

1. 项目开发受课程实验周期限制，需要在规定时间内完成需求分析、系统实现和测试验证。
2. 系统默认运行于本地离线环境，模型文件与数据集需提前准备完毕。
3. 本系统主要用于实验研究与课程展示，不考虑商业化部署需求。
4. 系统运行需要具备 Python 环境、Conda 环境及相关依赖库。
5. 模型训练和大规模推理对硬件有一定要求，若无 GPU 支持，运行效率会明显下降。
6. 本实验主要处理 Python 代码样本，其他编程语言暂不作为重点支持对象。
7. 水印检测采用统计判定方法，结果具有一定误差范围，无法保证在所有极端情况下完全准确。

## 2、需求规定

### 2.1、软件功能说明

本系统应具备以下主要功能：

1. 配置管理功能  
系统应支持通过配置文件或命令行参数设置模型路径、数据集路径、密钥、水印参数及输出路径等内容。

2. 编码器训练功能  
输入为本地代码数据集及训练参数；  
处理过程包括代码解析、语句块切分、样本构造与语义编码训练；  
输出为训练后的编码器模型及训练日志。

3. 水印嵌入功能  
输入为代码生成模型、语义编码器、密钥信息和任务数据；  
处理过程包括代码生成、语句块分析、水印判定及重采样控制；  
输出为含水印代码及相关元数据。

4. 水印提取功能  
输入为待检测代码、提取参数和密钥；  
处理过程包括代码解析、语义特征计算、统计检验和结果判定；  
输出为检测结果，包括是否检测到水印及相应评分信息。

5. 流程控制功能  
系统应支持按阶段单独运行，也应支持按完整流程顺序执行。

6. 结果保存与恢复功能  
系统应保存实验过程中的模型、中间结果和最终输出，并支持在中断后继续执行。

本系统主要面向单机实验环境，支持单用户在本地终端上完成实验操作，暂不考虑多用户并发场景。

### 2.2、对功能的一般性规定

为保证系统整体一致性，对各项功能提出如下统一要求：

1. 系统输入输出格式应尽量统一，主要采用 `json`、`jsonl` 和本地模型目录形式保存数据。
2. 系统应具备基本的参数合法性检查能力，对路径错误、参数缺失、格式异常等情况给出明确提示。
3. 各阶段运行过程应保留必要日志信息，便于后续调试与结果分析。
4. 系统目录结构和模块划分应保持统一，便于维护和扩展。
5. 发生异常时，应尽可能保留已有结果，避免重复计算造成时间浪费。
6. 系统的核心功能应能够在离线环境中独立完成。

### 2.3、用户界面

本系统采用命令行界面作为主要用户界面。用户通过输入命令及参数控制系统运行，系统通过终端输出运行状态、进度信息、错误提示和实验结果。

界面设计要求如下：

1. 操作方式简洁明确，适合课程实验环境使用。
2. 命令格式统一，便于记忆和重复执行。
3. 输出信息应清晰易读，方便用户了解当前执行情况。
4. 不设计复杂图形界面，将重点放在核心功能实现与实验验证上。

### 2.4、对性能的一般性规定

#### 2.4.1、精度

1. 编码器训练应能够较稳定地区分语义相近与语义不同的代码样本。
2. 水印嵌入后生成的代码应尽量保持语法正确、语义合理。
3. 水印提取结果应具有较好的判别能力，尽量降低误检率和漏检率。
4. 系统中涉及评分和阈值判断的数值计算应保留必要精度，避免数值误差影响实验结论。

#### 2.4.2、时间特性要求

1. 系统启动与配置加载应在较短时间内完成。
2. 单次检测任务应能在实验可接受时间内给出结果。
3. 编码器训练和水印嵌入过程允许耗时较长，但应提供必要的运行提示或日志输出。
4. 系统应支持中断恢复，以减少长时间任务重复执行的成本。

#### 2.4.3、灵活性

1. 系统应支持切换不同代码数据集，如 HumanEval、MBPP 等。
2. 系统应支持更换不同的代码生成模型和语义编码器模型。
3. 系统应支持调整训练参数、水印参数和提取阈值，以适应不同实验需求。
4. 当运行环境发生变化时，应尽量通过修改配置完成适配，而不是大规模修改源代码。
5. 系统应为后续扩展至其他编程语言或更复杂检测方法预留一定空间。

#### 2.4.4、输入输出要求

1. 输入内容  
系统输入主要包括配置文件、模型文件、代码数据集、命令行参数以及待检测代码文件。

2. 输入格式  
配置文件采用 `json` 格式；  
实验结果和样本记录主要采用 `jsonl` 格式；  
源代码文件采用 UTF-8 编码的文本格式。

3. 输入范围  
各项参数应设置在合理范围内，路径参数应指向有效文件或目录，密钥参数应符合系统预设要求。

4. 输出内容  
系统输出包括训练后的模型文件、含水印代码、检测结果、日志文件和中间状态文件。

5. 输出要求  
检测结果中应明确给出是否检测到水印，并附带必要的得分或判定依据，以支持后续实验分析。

### 2.5、数据管理能力要求

本系统需要管理的数据主要包括以下几类：

1. 基础模型文件和训练后模型文件；
2. 本地代码数据集；
3. 含水印代码输出文件；
4. 检测结果文件、日志文件及中间状态文件。

从数据规模来看：

1. 模型文件一般占用较大存储空间，可能达到数百 MB 甚至数 GB；
2. 数据集本身规模相对较小，通常为 MB 级；
3. 多轮实验将产生较多输出文件和结果记录，需要合理组织与分类存储；
4. 系统应按照模块划分目录，对模型、数据和结果进行统一管理，便于后续维护和复现实验。

### 2.6、故障处理要求

系统运行过程中可能出现以下故障情况，并应具备相应处理能力：

1. 模型文件或数据集路径错误时，系统应在运行初期进行检查并给出提示，避免进入错误流程。
2. 输入代码格式不合法或语法解析失败时，系统应记录异常信息，避免整体程序崩溃。
3. 参数设置不合理时，系统应给出明确的错误说明，提示用户修改。
4. 当出现内存不足或显存不足情况时，系统应提示用户调整参数或更换运行环境。
5. 运行任务中断后，系统应尽量保留已完成结果，并支持恢复执行。
6. 系统应记录关键故障信息，便于后续定位和分析问题。

### 2.7、其他专门要求

1. 安全性要求  
系统中的密钥信息不应在公共输出文件中直接暴露。

2. 可维护性要求  
程序应采用模块化设计，结构清晰，便于修改、调试和维护。

3. 可扩充性要求  
系统后续应能够方便地接入新的数据集、模型或检测策略。

4. 可读性要求  
代码、配置文件以及输出信息应尽量规范清晰，便于理解和使用。

5. 可靠性要求  
在输入正确、环境正常的条件下，系统应能够稳定完成主要实验流程。

6. 可移植性要求  
系统应尽量支持常见 Linux 环境，并具备一定跨平台适应能力。

## 3、运行环境规定

### 3.1、设备

本系统建议具备如下硬件条件：

1. 64 位计算机一台；
2. 多核 CPU；
3. 内存不少于 8GB，建议 16GB 及以上；
4. 若进行模型训练或较大规模推理，建议配备支持 CUDA 的 GPU；
5. 磁盘应保留足够空间用于保存模型、数据集和实验结果，建议不少于 20GB。

### 3.2、支撑软件

本系统运行所需支撑软件包括：

1. 操作系统：Linux；
2. Python 解释器：Python 3.11；
3. 环境管理工具：Conda；
4. 深度学习框架：PyTorch；
5. 相关依赖库：transformers、datasets、pytest 等；
6. 源代码管理工具：Git。

本系统主要通过本地文件进行数据管理，不依赖额外的数据库管理系统。

### 3.3、接口

本系统涉及的主要接口如下：

1. 命令行接口  
用户通过命令行输入控制系统运行阶段及相关参数。

2. 配置文件接口  
系统通过配置文件读取实验所需的模型参数、路径信息和控制参数。

3. 文件接口  
系统通过本地文件实现模型读取、数据集加载、结果保存和日志记录。

4. 模块接口  
系统内部各模块之间通过函数调用和统一数据格式完成信息交互。

### 3.4、控制

系统主要通过命令行方式进行控制。用户可通过不同命令实现以下操作：

1. 查看系统当前运行状态；
2. 执行完整实验流程；
3. 单独执行编码器训练阶段；
4. 单独执行水印嵌入阶段；
5. 单独执行水印提取与验证阶段；
6. 指定配置文件、模型路径、输出路径及相关控制参数。

控制信号主要来源于用户输入的命令行参数和配置文件内容，系统根据解析结果决定具体执行流程。

## 4、尚需解决的问题

在当前需求分析阶段结束后，仍有以下问题需要在后续实验中进一步研究和完善：

1. 不同代码生成模型对水印嵌入效果的影响仍需通过更多实验进行验证。
2. 水印提取阶段的阈值设置需要结合更多样本进行调优。
3. 系统对复杂代码变换及对抗性修改的鲁棒性仍需进一步分析。
4. 当前实验主要面向 Python 代码，后续是否支持多语言仍需进一步设计。
5. 在硬件资源有限的条件下，系统运行效率仍有优化空间。
6. 若系统后续扩展为更完整的软件产品，还需要进一步完善结果展示和可视化功能。
